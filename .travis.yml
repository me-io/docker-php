dist: trusty

sudo: required

language: ruby

services:
  - docker

cache:
  bundler: true
  directories:
    - $HOME/docker

#before_script:
#  - do something :)

env:
  - BUILD_CMD="php80/build.sh f"
  - BUILD_CMD="php74/build.sh f"
  - BUILD_CMD="php73/build.sh f"
script: ./${BUILD_CMD}

#after_success:
#- update the readme and tags etc...
